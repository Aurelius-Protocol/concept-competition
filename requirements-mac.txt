# Local Apple-Silicon (MPS) runtime deps for running the scorer on a Mac GPU.
#
# This is NOT the pinned CUDA image set (see requirements.txt, used by the Dockerfile).
# Differences that matter on Apple Silicon:
#   * NO bitsandbytes  — 4-bit NF4 is CUDA-only; on MPS we run the model unquantized in bf16.
#   * recent torch     — needs an arm64/MPS build new enough for this Mac (M-series),
#                        not the CUDA-pinned torch==2.5.1.
#   * recent transformers — must support Gemma 3 and the `dtype=` from_pretrained kwarg
#                        (>=4.56); verified working on MPS with 5.9.0.
#
# Resolve to current stable wheels (Python 3.12 recommended; see README "Apple Silicon").
torch>=2.7
transformers==5.9.0   # pinned to the MPS-verified version (dev-only; bump if a newer Mac needs it)
accelerate>=1.3
safetensors>=0.4.5
huggingface_hub>=0.27
datasets>=3.2
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pyyaml>=6
python-multipart>=0.0.20
openai>=1.30        # OpenAI-compatible client for the LM Studio (black-box) backend
