# CUDA-only extras for the high-throughput vLLM backend (CONCEPT_SCORER_BACKEND=vllm).
#
# NOT part of requirements.txt: vLLM has no Apple/MPS wheel and pins its own torch/CUDA stack,
# so installing it would break the Mac dev install. Install this ONLY on the CUDA verification
# box, in a fresh env, and let vLLM resolve its own compatible torch (do not layer it over the
# requirements.txt torch==2.5.1 pin):
#
#     pip install -r requirements-vllm.txt
#
# vLLM added Gemma-3 text support in v0.8.0. Pinned to the version verified end-to-end on CUDA
# (RTX 4090, gemma-3-12b-it NF4 weather smoke, 2026-06-02) — see SPEC §2 for the full stack.
# vLLM resolves its own torch (2.11.0+cu130) and transformers (5.9.0); install `bitsandbytes`
# alongside for the NF4 path (CONCEPT_SCORER_VLLM_QUANTIZATION=bitsandbytes).
vllm==0.22.0
